# Postgres connection string for the newsletter subscribers table (Railway sets this
# automatically once the Postgres service is linked to this service).
DATABASE_URL=

# Gmail account used to send confirmation/unsubscribe emails. Requires 2-Step
# Verification on the Google account, with an App Password generated at
# https://myaccount.google.com/apppasswords (NOT the regular account password).
GMAIL_USER=sxe.science@gmail.com
GMAIL_APP_PASSWORD=
